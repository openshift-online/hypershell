import { ClientsPage } from "../features/shell/shell-pages";
import { createPageMeta } from "../lib/page-meta";

export const meta = createPageMeta(
  "app.nav.clients",
  "app.page.clients.description",
);

export default ClientsPage;
