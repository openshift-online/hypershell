import { SettingsPage } from "../features/shell/shell-pages";
import { createPageMeta } from "../lib/page-meta";

export const meta = createPageMeta(
  "app.nav.settings",
  "app.page.settings.description",
);

export default SettingsPage;
