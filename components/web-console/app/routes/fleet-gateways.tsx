import { GatewaysPage } from "../features/shell/shell-pages";
import { createPageMeta } from "../lib/page-meta";

export const meta = createPageMeta(
  "app.nav.gateways",
  "app.page.gateways.description",
);

export default GatewaysPage;
