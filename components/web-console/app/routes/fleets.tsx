import { SectorsPage } from "../features/shell/shell-pages";
import { createPageMeta } from "../lib/page-meta";

export const meta = createPageMeta(
  "app.nav.sectors",
  "app.page.sectors.description",
);

export default SectorsPage;
