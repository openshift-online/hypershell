import { AdminClustersPage } from "../features/shell/shell-pages";
import { createPageMeta } from "../lib/page-meta";

export const meta = createPageMeta(
  "app.nav.clusters",
  "app.page.clusters.description",
  "app.adminProductName",
);

export default AdminClustersPage;
