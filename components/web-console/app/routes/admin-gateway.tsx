import { AdminGatewayPage } from "../features/shell/shell-pages";
import { createPageMeta } from "../lib/page-meta";

export const meta = createPageMeta(
  "app.page.adminGateway.title",
  "app.page.adminGateway.description",
  "app.adminProductName",
);

export default AdminGatewayPage;
