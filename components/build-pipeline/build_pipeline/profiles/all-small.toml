# All non-tool-only steps run on the small/cheap model. Baseline for cost.
name = "all-small"

[tiers.small]
provider = "vertex"
model = "gemini-2.5-flash"
[tiers.small.params]
temperature = 0
price_in_per_m = 0.30
price_out_per_m = 2.50

[tiers.standard]
provider = "vertex"
model = "gemini-2.5-flash"
[tiers.standard.params]
temperature = 0
price_in_per_m = 0.30
price_out_per_m = 2.50

[tiers.deep]
provider = "vertex"
model = "gemini-2.5-flash"
[tiers.deep.params]
temperature = 0
price_in_per_m = 0.30
price_out_per_m = 2.50

[overrides]
