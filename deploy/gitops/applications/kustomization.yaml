apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# OpenShift application package for a namespace-restricted Argo CD instance.
# Environment overlays must provide configuration and immutable image digests.
resources:
  - ../../base/applications
  - route.yaml
