apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

# Namespaced API and console resources. The privileged controller is deliberately
# kept with its ServiceAccount and RBAC in ../platform-resources.
resources:
  - api-server.yaml
  - web-console.yaml
