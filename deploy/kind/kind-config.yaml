kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      - containerPort: 443
        hostPort: 443
        protocol: TCP
    extraMounts:
      - hostPath: __KIND_HOST_MOUNT_PATH__
        containerPath: /mnt/host
        readOnly: false
