apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component
resources:
  - metrics-access.yaml
patches:
  - target:
      kind: Deployment
      name: hypershell-web-console
    path: web-console.yaml
