# Python bytecode and caches
__pycache__/
*.py[cod]
*$py.class
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Python virtual environments
.venv/
venv/
env/

# JavaScript dependencies and generated artifacts
node_modules/
.pnpm-store/
.cache/
.local/
.npm/
.react-router/
build/
# The generic build/ ignore above also matches the specs/tooling/build spec
# directory; keep that tracked (it holds specs, not build output).
!specs/tooling/build/
coverage/
dist/
playwright-report/
storybook-static/
test-results/
perf-results/
*.tsbuildinfo

# Agent Package Manager cache
apm_modules/

# Claude Code working state
.claude/

# Kind swap tracking
.kind-swaps

# OpenShift per-namespace swap tracking
.openshift-swaps/

# Temporary kustomize wrap dirs used by openshift-up
.openshift-render.*/

# Local tool binaries (built by make kind-prereqs)
bin/

# Generator build artifacts
scripts/sdk-generator/sdk-generator
scripts/cli-generator/cli-generator
components/api-server/hypershell
components/cli/hypershell
GAP_ANALYSIS.md
RECONCILE_COMPLETE.md
loop.txt
/components/cli/hsctl

# JetBrains IDE project files
.idea/
