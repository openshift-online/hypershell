package revoke

import (
	"github.com/spf13/cobra"

	"{{.Module}}/cmd/{{.Binary}}/revoke/serviceAccount"
)

var Cmd = &cobra.Command{
	Use:   "revoke RESOURCE ID",
	Short: "Permanently revoke a resource",
}

func init() {
	Cmd.AddCommand(serviceAccount.Cmd)
}
